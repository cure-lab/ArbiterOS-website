# ArbiterOS Website

Landing page for ArbiterOS product messaging.

## Local preview

Open `index.html` directly in browser, or run any static server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
